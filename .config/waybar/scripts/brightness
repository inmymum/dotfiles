while true; do
    output=$(printf "Done\n5%%+\n5%%-" | fuzzel -d)
    for i in $output; do
        if [[ $i == "Done" ]]; then
            exit 0
        fi
        brightnessctl --class=backlight set "$i"
    done
done
